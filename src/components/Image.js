import styled from 'styled-components';
import { space, color, layout, position, zIndex, size } from 'styled-system';

const Image = styled.img`
  ${space}
  ${color}
  ${layout}
  ${position}
  ${zIndex}
  ${size}
`;

export default Image;