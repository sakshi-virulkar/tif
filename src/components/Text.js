import styled from "styled-components";
import { color, typography , space, layout} from "styled-system";

const Text = styled.p`
color : black;
display : block;
font-size : 20px;



${color}
${typography}
${space}
${layout}


`

export default Text